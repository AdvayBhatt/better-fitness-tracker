// Native: renders nothing, the review tool's page detection only matters on web.
export default function PageMarker(_props: {
  id: string;
  name: string;
  description?: string;
}) {
  return null;
}
